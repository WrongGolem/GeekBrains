a = 15 * 3  # создаем переменные для формул
b = 15 / 3
c = 15 // 2
d = 15 ** 2

print(a, '-', type(a))  # используем метод type
print(b, '-', type(b))  # используем метод type
print(c, '-', type(c))  # используем метод type
print(d, '-', type(d))  # используем метод type
